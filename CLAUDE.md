# Project Rules

## Git Rules

- **NEVER push code to the remote repository until explicitly told to by the user.**
- Always wait for the user to review and test code before committing or pushing.
- Do not commit or push automatically after completing a task — ask first.
